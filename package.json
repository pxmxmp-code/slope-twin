{
  "name": "slope-twin",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -k -n web,api \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "npm run dev --workspace @slope-twin/web",
    "dev:api": "uv run --project apps/api uvicorn app.main:app --app-dir apps/api --host 0.0.0.0 --port 8000 --reload --reload-dir apps/api/app",
    "start:api": "uv run --project apps/api uvicorn app.main:app --app-dir apps/api --host 0.0.0.0 --port 8000",
    "build": "npm run build --workspace @slope-twin/web",
    "start:web": "npm run start --workspace @slope-twin/web",
    "typecheck": "npm run typecheck --workspace @slope-twin/web",
    "test:api": "uv run --project apps/api pytest apps/api/tests -q",
    "test:e2e": "playwright test",
    "format": "prettier --write apps/web package.json playwright.config.ts tests"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "@playwright/test": "^1.58.0",
    "prettier": "^3.6.2"
  }
}
