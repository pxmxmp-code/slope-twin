# Project Agent Instructions

- Always prefix shell commands with `rtk`.
- Unless the user explicitly requests browser testing, do not launch a browser or run Playwright/E2E browser tests. Use type checks, unit/API tests, and production builds for routine verification.
